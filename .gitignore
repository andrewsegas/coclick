# Python
__pycache__/
*.py[cod]

# config.ini holds per-machine positions AND the user's Discord webhook
# (a secret) — never commit it. It is regenerated on first run.
config.ini
configbkp.ini

# debug_ocr.py output (personal screenshots)
debug_ocr/

# Local / misc
*.rar
